// Contacts
    // Render nach Initialen
        // mit contactDb arbeiten 
        // reduce scheint der way to go? als parameter leeres objekt, syntax ergooglen mit accumulator {}
        // dadurch neues objekt "sortedContacts" oder so erzeugen?
            // prüfen ob initiale schon besteht, falls ja verwenden falls nein leeres array erstellen
        // 2 For schleifen zum rendern

// Login:
    // bauen mit if pw === x / else?

// forms:
    // form validation hinzufügen?

// global:
    // rnd farben an kontakte übergeben, in firebase speichern
    // wie an tasks übergeben? als key value task:assigned[contact{name: x, color: y,}] 

/// BONUS WENN ZEIT
    // Update und Performance:
        // Funktion um nur die jeweilige Karte zu Updaten nach edit (find / findindex oder so?)
    // board:
        // buttons neben den überschriften (low, medium, urgent etc) um neuen task anzlegen?
            // muss man mit popup machen :S also vielleicht eher nicht
            // oder so ein formular iwo versteckeln
        // assigned im board löschbar machen ohne dass kontakt gelöscht wird
        // Wenn Kontakt editiert wird muss dieser auch im task bei assigned editiert werden

->> Allgemein Array Methoden vertiefen (reduce, sort, map, filter, object.entries?)
->> Rausfinden was man mit strings alles anstellen kann LOL 
->> Arrow Functions vertiefen
->> For Each Schleifen verwenden?
->> weiter stylen wenn bock, eher kein Bock
