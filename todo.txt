// tasks variablen umbenennen

// Board:
    // drag and drop
        //  wie status ändern?
    // subtasks hinzufügen?

// forms:
    // form validation
    // mehrere assigns ermöglichend durch mehrfachauswahl, array in object struktur? querySelector.all

// Contacts
    // Initialen bestimmen in globaler js
    // farben
    // Render nach Initialen -> mit contactDb arbeiten, 2 For schleifen

// Render:
    // Funktion um nur die jeweilige Karte zu Updaten nach edit (find ?)

// login 
    // bauen mit if pw === x / else?

// ->> Allgemein Array Methoden vertiefen

// weiter stylen
